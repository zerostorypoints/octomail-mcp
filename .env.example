# OAuth client credentials from your Google Cloud Desktop app client.
GOOGLE_CLIENT_ID=your-google-oauth-desktop-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# Instead of the two variables above, you may point at a downloaded OAuth JSON file:
# GOOGLE_OAUTH_CREDENTIALS_FILE=/absolute/path/to/credentials.json

# Optional. Defaults to ./accounts.json.
# OCTOMAIL_ACCOUNTS_FILE=/absolute/path/to/accounts.json

# Optional. Defaults to ~/.octomail/tokens.
# OCTOMAIL_TOKEN_DIR=/absolute/path/to/tokens

# Optional. Defaults to ~/.octomail/attachments.
# Attachments fetched by gmail_get_attachment are written to a per-account
# subdirectory beneath this path, and outbound file attachments must live there.
# OCTOMAIL_DOWNLOAD_DIR=/absolute/path/to/attachments
